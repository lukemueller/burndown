---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk'

inputs:
- name: burndown-git
outputs:
- name: burndown-jar-with-dependencies

run:
  path: burndown-git/ci/tasks/test.sh