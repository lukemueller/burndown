resources:
  - name: source-code
    type: git
    source:
      uri: https://github.com/lukemueller/burndown.git
      branch: concourse

jobs:
  - name: test
    plan:
    - get: source-code
      trigger: true
    - task: test
      file: source-code/ci/tasks/test.yml